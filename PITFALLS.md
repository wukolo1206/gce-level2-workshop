# PITFALLS.md — 踩坑與地雷記錄

## ⚠️ 已知風險（尚未修復）
- `docs/` 底下數份舊講義 .md 內仍嵌著範本檔的 `/edit` 網址。權限已降為 reader 所以改不到母版，
  但學員點進去會是唯讀狀態、無法動手做，體驗不一致。建議日後一併改為 `/copy`。

---

## 範本檔公開權限預設是 writer，等於把母版開放給全世界編輯

**現象**：2026-08-16 以 Drive API 實查 25 個研習範本檔，`anyone` 權限全是 **writer**（知道連結者可編輯）。
**原因**：當初用 API 建檔時直接給了 writer，方便自己測試，之後沒有收回。
**風險**：任何拿到 `/edit` 網址的人都能改動母版；研習現場數十人同開一份更會互相覆蓋。
**解法**：全部降為 `reader`（`permissions().update(role='reader')`），並確認 `copyRequiresWriterPermission=false`
以免連 `/copy` 也一起被鎖。降權前的狀態已存於 `permissions_snapshot_before_reader.json` 可還原。
講義中的觀摩型連結一律改用 `/preview`（唯讀檢視），動手型連結才用 `/copy`。
**未來避免**：用 API 大量建立要對外分享的檔案時，權限一律預設 `reader`，需要編輯再個別開。

## 已踩到的坑

## 研習範本檔不能給 /edit 連結，要給 /copy

**現象**：講義中每則演練都掛一個 `…/edit?usp=sharing` 的「真實實作檔」連結。研習現場數十人同時點開，
所有人會落在<strong>同一份檔案</strong>上互相覆蓋；若權限只給檢視，則根本無法動手做。
**原因**：把「觀摩用的示範檔」與「每人各自要改的練習檔」混為一談。
**解法**：網址結尾的 `/edit?usp=sharing` 一律改為 `/copy`，點擊後會提示建立副本，
學員在自己的雲端硬碟作業（Docs／Sheets／Slides／Forms 皆適用）。
**未來避免**：任何要發給多人「動手改」的雲端檔案，一律發 `/copy` 連結；純觀摩用則發 `/preview`。

---

## 非檔案型工具（Calendar／Meet／Classroom／Sites）不能用「開一份 Docs」當練習環境

**現象**：Calendar、Meet、Sites 等演練的綠框，掛的是一份寫著操作說明的 Google Docs。
學員點開只是「讀」，完全沒有練到該工具本身。
**原因**：25 個範本檔當初一律以 Docs 產生，未區分工具性質。
**解法**：改依工具性質給練習環境——Calendar 用 `calendar.google.com/calendar/render?action=TEMPLATE&text=…&dates=…&details=…`
預填活動網址（直接在學員自己的日曆開草稿）；Classroom 協同教師改兩人互邀；Meet 分組討論室須自己當主持人；
Sites 沒有公開複製機制，改為觀摩示範站後自建。
**未來避免**：設計研習實作前先問「這個功能一個人、只有一份文件，練得起來嗎？」

## PowerShell 執行內嵌 Python 腳本時符號轉義問題

**現象**：在 Windows PowerShell 呼叫 `python -c "..."` 時，若字串中包含 `&` 或 CSS 雙重波浪符號（如 `--text-muted`），PowerShell 會將其誤判為背景運算子或語法錯誤。  
**原因**：PowerShell 的命令解析器對 `&` 與 `"` 的處理機制與 CMD / Linux bash 不同。  
**解法**：一律將 Python 生成腳本寫入獨立 `.py` 檔案（例如 `build_quiz_app.py`、`build_lab_exercises_app.py`），再透過 `python build_xxx.py` 執行。  
**未來避免**：避免撰寫過長且含特殊 CSS/HTML 符號的內嵌 `python -c` 命令。

---

## Lab Exam 自動評分文字精確度坑

**現象**：在 Google 官方認證控制台的 Check my progress 驗證中，輸入中文課程名稱或非精確英文名稱會直接被判定為 0 分。  
**原因**：Google 官方自動評分腳本（Automated scoring script）採字串精確比對 (Case-sensitive exact string matching)。  
**解法**：在講義與網頁 App (`lab_exercises_app.html`) 中新增「📋 複製文字」按鈕，確保學員練習時複製原版英文（如 `Flipped Class`, `Community Fair`, `123 Main Street`）。  
**未來避免**：凡涉及 Lab 實作題目的英文關鍵字，一律提供精確的英文文字複製按鈕。

---

## 講義寫的按鈕名稱與實際 UI 不符（會議記錄 / 邀請對象權限）

**現象**：學員照講義找「新增會議紀錄 (Add meeting notes)」，在活動編輯畫面上完全找不到。
**原因**：兩個錯誤——① 正確名稱是「**建立會議記錄** (Create meeting notes)」；
② 它<strong>不是說明欄上方的文字連結，而是說明欄工具列最左邊的 Google 文件三角形圖示</strong>（在 B I U 左側）。
同時「與會者權限／邀請他人／查看與會者名單」的實際 zh-TW 介面字串是
「**邀請對象權限／邀請其他使用者／查看邀請對象名單**」。
**解法**：以官方說明文件（support.google.com/docs/answer/11324079）為準改寫，並在講義中直接標明圖示位置。
**未來避免**：寫「點選某某按鈕」之前先查官方說明頁確認**當前**名稱與位置；
Google 的 UI 文案改版頻繁，憑印象寫的選單路徑是研習現場最容易當場破功的地方。

---

## 串流直播與 Practice Sets 同屬付費授權功能

**現象**：Calendar 演練二要求「加開串流直播」，但多數帳號根本看不到這個選項。
**原因**：Live stream 需 Education Plus 或 Teaching and Learning Upgrade（與 Practice Sets 同一級），
個人 Gmail 與 Education Fundamentals 皆無。
**解法**：在該演練加註授權門檻與替代作法，並點明**考試考的是「大量觀眾只觀看時該用直播」這個判斷**，
而非帳號有沒有那個按鈕。
**未來避免**：設計實作演練時，先確認該功能的授權層級；Education Plus 專屬功能一律標註並附替代練習。

---

## 憑印象寫 Google 介面名稱，兩輪審查抓出 12 處錯誤

**現象**：講義中的按鈕與選單名稱大量寫錯——主題建構工具（實為主題製作工具）、直行統計（資料欄統計資料）、
檢視（實為查看）、回應分頁（回覆）、班級洞察（課程深入分析）、排定時間（安排時間）等。
**原因**：憑訓練資料印象或舊版介面書寫，未逐一查官方繁中說明頁。
**解法**：交叉審查（Codex）＋官方說明頁逐條查證；並產出 `實測檢核表.md`（94 項）供人工實機核對。
**未來避免**：凡是寫「點選某某按鈕」的教學文件，動筆前先查官方繁中說明頁；
Google 的 UI 文案改版頻繁，且**繁中翻譯與英文原名不是一對一**（例：Filter views = 篩選器檢視畫面）。

---

## 改一半的修訂比不改更危險

**現象**：第一輪審查後把 Classroom 公告演練的「指定任務」改成發兩則公告，卻沒同步改「操作步驟」，
導致步驟只建一則、且從未按「張貼」，最後一步卻要求對「已張貼的公告」操作——照做必然卡住。
同一輪還把「回應驗證」連坐誤改為「回覆驗證」（回覆只是結果分頁名稱）。
**原因**：批次字串取代時只改了任務描述，未檢查同一則演練的其他欄位是否一致。
**解法**：送第二輪審查抓出來。修訂後應整則重讀一次，確認 intro／scenario／todo／steps 四個欄位彼此不矛盾。
**未來避免**：批次修訂後務必再送一輪審查——第二輪的價值就在抓第一輪引入的新錯誤（本次抓到 3 個）。

---

## 同一帳號不能在同一課程同時是協同教師與學生

**現象**：Classroom 演練設計為「演練一互加協同教師 ➔ 演練三請夥伴以學生身分作答測驗」，
但夥伴已是該課程的協同教師，無法再以學生身分加入。
**解法**：需要學生視角的演練改為「另開一門沒有加對方為協同教師的測試課程」，或使用第三個帳號。
**未來避免**：設計連續演練時，檢查前一則建立的身分／權限狀態會不會擋住後一則。

---

## 公開版夾帶真實考題截圖與帳號頭像

**現象**：v3.1.0 明訂「公開版排除考場實景截圖與報名教學」，但後續 session 新增
`exam_registration.html` 並把 `actual_exam_screen.png` 加進公開打包白名單，政策被反轉且無決策紀錄。
該圖含**完整可讀的官方第 1 題題幹**與右上角**帳號頭像「國榮」**；`switch_language_menu.png` 也帶同一頭像。
自公開 repo commit `cc82fba` 起即已上線。

**原因**：公開白名單是逐檔手工列舉，加圖時只確認「頁面需要這張圖」，沒有檢查圖**內容**是否含考題或個資；
CHANGELOG 的排除政策寫在文件裡，建置腳本沒有任何機制強制執行。

**解法**：新增 `scripts/mask_exam_screenshot.py` 產生 `*_masked.png`（模糊題幹、灰圓覆蓋頭像），
`build_public_index.py` 對這兩張圖一律複製打碼版並沿用原檔名；
**缺打碼檔時 `raise SystemExit` 中止建置**，避免靜默退回原圖。

**未來避免**：
1. 任何截圖進公開白名單前，**實際開圖看過**——看的是像素內容，不是檔名。
2. 重點檢查兩類：可讀的官方題幹、帳號頭像／Email／姓名。
3. 政策若要反轉（如 v3.1.0 的排除清單），先寫進 DECISIONS.md 再改腳本。
4. ⚠️ **已公開的東西刪不乾淨**：本次替換只換掉線上檔案，
   未打碼原圖仍留在公開 repo 的 git 歷史（`cc82fba` 等 commit）中可被取出，
   徹底清除需改寫歷史並強制推送。
